import { createContext } from "react";

const AuthProvider = createContext(null)

const AuthProvider = () => {
       return (
              <div>
                     
              </div>
       );
};

export default AuthProvider;